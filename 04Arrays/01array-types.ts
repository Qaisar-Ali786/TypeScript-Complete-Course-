// Array types ?
// arrays are a type of object that can store multiple values of same data type. arrays in javascript
// are typed which mean you can specify the type of values that an array can hold.

// types of array

// 1.using the square bracket notation[] to indicate an array of specific type
// 2.using the generic array <type> to indicate an array of specific type.

const data: string[] = ["name", "age", "gender"];
data.push("male");
console.log(data);

const num: number[] = [1, 2, 3, 4, 5];
console.log(num);
