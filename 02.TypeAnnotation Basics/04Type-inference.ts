// What is type-inference
// the ability of compiler to automatically deduce the type of variable, function return, and expression
// even if you dont explicity specify it.

let tech = "typescript";
// we cannot write
//  tech = 12
// if we write the compiler says that
// the value of tech should be in string
let favNumber = 0;
let isHard = true;

console.log(tech, favNumber, isHard);
